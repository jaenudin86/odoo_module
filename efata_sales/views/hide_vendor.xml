<odoo>
    <data>
        <!-- Misalnya untuk membuat rule yang hanya memperlihatkan customer berdasarkan user -->
        <!-- <record id="sales_user_partner_rule" model="ir.rule">
            <field name="name">Sales User Can See Their Customers Only</field>
            <field name="model_id" ref="model_res_partner"/>
            <field name="domain_force">[('users_visible', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
        </record> -->

        <!-- Contoh view dengan domain filter (tree atau kanban) -->

    </data>
</odoo>
