<odoo>
    <data>
        <!-- Inherit List View -->
        <record id="view_partner_tree_inherit_users_visible" model="ir.ui.view">
            <field name="name">res.partner.tree.user.visible</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_tree"/>
            <field name="arch" type="xml">
                  <xpath expr="//field[@name='email']" position="after">
                    <field name="users_visible"/>
                </xpath>
                <xpath expr="//list" position="attributes">
                    <attribute name="groups">base.group_user</attribute>
                    <attribute name="domain">['|', ('users_visible', '=', uid), ('users_visible', '=', False)]</attribute>
                </xpath>
            </field>
        </record>

        <!-- Inherit Kanban View -->

    </data>
</odoo>
