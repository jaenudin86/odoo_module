<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_product_product_form_inherit" model="ir.ui.view">
        <field name="name">product.product.form.inherit.default_code</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
                <field name="default_code" position="replace">
                    <!-- Tambahkan atribut groups jika perlu -->
                </field>

                <!-- Tambahkan default_code setelah invoice_policy -->
        <field name="invoice_policy" position="after">
            <field name="default_code" modifiers="{'invisible': [('show_default_code', '=', False)]}"/>
        </field>

        <field name="barcode" position="attributes">
                    <attribute name="invisible">1</attribute>
        </field>
        <xpath expr="//form" position="attributes">
            <attribute name="string">Informasi Produk Baru</attribute>
        </xpath>
            <xpath expr="//field[@name='name']" position="attributes">
                <attribute name="placeholder">Product's Code...</attribute>
            </xpath>
        </field>
    </record>
</odoo>
